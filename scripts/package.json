{
  "name": "lobstertrack-scripts",
  "private": true,
  "type": "module",
  "description": "Standalone Node/TS tooling that talks to production Postgres directly — not part of the frontend or backend app, run manually via `npx tsx <script>.ts`.",
  "scripts": {
    "backtest": "tsx backtest.ts"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
